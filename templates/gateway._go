package gateway

type {{CamelCase .GatewayName}}Gateway struct {
}

// New{{PascalCase .GatewayName}}Gateway ...
func New{{PascalCase .GatewayName}}Gateway() *{{CamelCase .GatewayName}}Gateway {
	return &{{CamelCase .GatewayName}}Gateway{}
}

{{range .Methods}}
func (r *{{CamelCase $.GatewayName}}Gateway) {{.MethodName}}{{.MethodSignature}} {
    log.InfoRequest(ctx, "called")

    return {{.DefaultReturnVal}}
}
{{end}}