package main

import (
	"flag"
	"{{.PackagePath}}/application"
	"{{.PackagePath}}/application/registry"
)

func main() {

	flag.Parse()
	switch flag.Arg(0) {

	// case "firstService":
	//   application.Run(registry.NewOrderServiceRegistry())

	// case "secondService":
	//   application.Run(registry.NewOrderServiceRegistry())

	default:
		application.Run(registry.New{{PascalCase .RegistryName}}Registry())
	}

}