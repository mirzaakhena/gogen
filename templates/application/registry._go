package application

import (
	"{{.PackagePath}}/controller/restapi"
	"{{.PackagePath}}/datasource/production"
	"{{.PackagePath}}/usecase/menuactivation"
)

func (a *Application) RegisterUsecase() {
	menuActivation(a)
	// CODE_INJECTION function_called
}

func menuActivation(a *Application) {
	outport := production.NewMenuActivationDatasource()
	inport := menuactivation.NewMenuActivationUsecase(outport)
	a.Router.POST("/menuactivation", restapi.MenuActivation(inport))
}

// CODE_INJECTION function_declared
