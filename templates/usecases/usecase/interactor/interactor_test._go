package interactor

import (
	"context"
	"testing"

	"{{.PackagePath}}/datasource/mocks"
	"{{.PackagePath}}/usecases/{{LowerCase .Name}}/inport"
	"{{.PackagePath}}/usecases/{{LowerCase .Name}}/outport"

	"github.com/stretchr/testify/assert"
)


func Test_Normal(t *testing.T) {

	ctx := context.Background()

	req := inport.{{PascalCase .Name}}Request{}

	outputPort := mocks.{{PascalCase .Name}}{}
  {{range .Outports}}
	outputPort.On("{{PascalCase .Name}}", ctx, outport.{{PascalCase .Name}}Request{ // 
	
	}).Return(nil, nil) 
  {{end}}
	res, err := New{{PascalCase .Name}}Inport(&outputPort).Handle(ctx, req)

	assert.Nil(t, err)

	assert.Equal(t, &inport.{{PascalCase .Name}}Response{ // 
	
	}, res)

}
