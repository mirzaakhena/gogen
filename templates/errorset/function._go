package errorset

import "fmt"

type ErrorCodeType int

func Err(code ErrorCodeType, args ...interface{}) error {
	messageWithCode := fmt.Sprintf("[E%d] %s", code, message[code])
	return fmt.Errorf(messageWithCode, args...)
}
