package {{LowerCase .Name}}

import (
	"context"

	"{{.PackagePath}}{{.Directory}}/usecase/{{LowerCase .Name}}/port"
)

// {{PascalCase .Name}} ...
func {{PascalCase .Name}}(inputPort port.{{PascalCase .Name}}Inport) func(interface{}) interface{} {

	return func(req interface{}) interface{} {

		// convert to request
		var req port.{{PascalCase .Name}}Request

		// run the usecase
		ctx := context.Background()
		res, err := inputPort.Handle(ctx, req)
		if err != nil {
			return nil
		}

		return res

	}
}
