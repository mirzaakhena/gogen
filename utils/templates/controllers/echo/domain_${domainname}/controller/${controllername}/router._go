package {{LowerCase .ControllerName}}

import "net/http"

func (r *controller) RegisterRouter() { {{range .Usecases}} {{if StartWith (LowerCase .Name) "get"}}
    resource := r.Router.Group("/api/v1")
    resource.Add(http.MethodGet , "/{{LowerCase .Name}}", r.authorization(r.{{CamelCase .Name}}Handler())) {{else}}
    resource.Add(http.MethodPost, "/{{LowerCase .Name}}", r.authorization(r.{{CamelCase .Name}}Handler())) {{end}} {{end}}
}
