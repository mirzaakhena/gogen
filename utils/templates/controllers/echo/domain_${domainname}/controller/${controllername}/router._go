package {{LowerCase .ControllerName}}

import "net/http"

func (r *echoController) RegisterRouter() { {{range .Usecases}} {{if StartWith (LowerCase .Name) "get"}}
    r.Router.Add(http.MethodGet , "/{{LowerCase .Name}}", r.authorization(r.{{CamelCase .Name}}Handler())) {{else}}
    r.Router.Add(http.MethodPost, "/{{LowerCase .Name}}", r.authorization(r.{{CamelCase .Name}}Handler())) {{end}} {{end}}
}
