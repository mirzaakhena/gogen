package errorenum

import "{{.PackagePath}}/shared/model/apperror"

const (
	FailUnmarshalResponseBodyError apperror.ErrorType = "ER1000 Fail to unmarshal response body"   // used by controller
	EntityNotFound                 apperror.ErrorType = "ER1001 Entity %s with id %s is not found" // used by injected repo in interactor
	UnrecognizedEnum               apperror.ErrorType = "ER1002 %s is not recognized %s enum"      // used by enum
	DatabaseNotFoundInContextError apperror.ErrorType = "ER1003 Database is not found in context"  // used by repoimpl
)
const NameMustNotEmpty apperror.ErrorType = "ER1000 name must not empty"                //
const RandomIDMust4CharLength apperror.ErrorType = "ER1000 random id must4 char length" //
const InvalidDate apperror.ErrorType = "ER1000 invalid date"                            //
const SomethingHappen apperror.ErrorType = "ER1000 something happen"                    //
